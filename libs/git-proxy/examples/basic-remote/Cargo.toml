[package]
name = "basic-remote"
version = "0.1.0"
edition = "2021"

[dependencies]
# web
axum = "0.7"
# logging
log = "0.4"
env_logger = "0.10"
# async
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net"] }
# workspace members
git-proxy = { path = "../.." }
