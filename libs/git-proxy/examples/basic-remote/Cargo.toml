[package]
name = "basic-remote"
version = "0.1.0"
edition = "2021"

[dependencies]
# web
axum = "0.8.8"
# logging
log = "0.4.29"
env_logger = "0.11.9"
# async
tokio = { version = "1.49.0", features = ["rt-multi-thread", "macros", "net"] }
# workspace members
git-proxy = { path = "../.." }
